export class Parent {
  sum(a, b) {
    return a + b;
  }
  sub(a, b) {
    return a - b;
  }
}

// const parentObj = new Parent();
// console.log(parentObj.sum(10,20));
// console.log(parentObj.sub(10,20));
